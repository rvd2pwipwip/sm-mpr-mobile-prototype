/**
 * Max content cards visible in a horizontal swimlane before the More affordance.
 * More shows when `sourceCount > SWIMLANE_CARD_MAX` (e.g. 13+ items at default 12).
 */
export const SWIMLANE_CARD_MAX = 12;
