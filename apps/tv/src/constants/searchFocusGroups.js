/** Vertical focus groups on the Search & Browse screen. */
export const SEARCH_FOCUS = Object.freeze({
  searchRow: 0,
  browseTabs: 1,
  /** First music browse body group (pills / genre row). */
  bodyStart: 2,
});
