/** Shared vertical focus groups on Home (header + banner + swimlanes). */
export const HOME_HEADER_GROUP = 0;
export const HOME_BANNER_GROUP = 1;
export const HOME_FIRST_SWIMLANE_GROUP = 2;

/** Landing focus on Home load — first card in the first card swimlane row. */
export const HOME_LANDING_ITEM_INDEX = 0;
